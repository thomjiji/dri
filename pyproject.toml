[tool.poetry]
name = "dri"
version = "0.0.1"
description = "DaVinci Resolve API type system for script development"
authors = ["thomjiji <hxy449999@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^7.3.2"

[tool.poetry.group.dev.dependencies]
black = {extras = ["d"], version = "^23.3.0"}
jupyter = "^1.0.0"
notebook = "^6.5.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"